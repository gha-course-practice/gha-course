name: Concurrency 1
on:
  workflow_dispatch:
    inputs:
      environment:
        description: "Environment"
        type: environment
        required: true
# this can be used at workflow or job level. Here is an example of workflow level
concurrency: concurrency-group-1
jobs:
  sleep:
    runs-on: ubuntu-latest
    steps:
      - run: sleep 30